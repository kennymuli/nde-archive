# GitHub Pages root

`index.html` here is a copy of `nde-archive/site/index.html`, which is the
source of truth. GitHub Pages can only serve from the repository root or
/docs on a branch deployment, hence the copy.

To refresh after editing the source:

    cp nde-archive/site/index.html docs/index.html
